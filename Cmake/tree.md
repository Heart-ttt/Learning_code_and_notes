# `tree` 命令使用指南

`tree` 是一个在终端中以**树状结构**显示目录和文件的命令行工具，直观清晰，常用于查看项目结构、汇报目录层级等。

> ⚠️ 注意：macOS 和部分 Linux 发行版默认**未安装 `tree`**，需手动安装（见下文）。

## 一、基础用法

| 命令 | 说明 |
|------|------|
| `tree` | 显示当前目录的完整树形结构 |

## 二、常用选项

| 选项 | 作用 |
|------|------|
| `-L N` | 限制显示深度为 N 层（如 `-L 2`） |
| `-d` | 仅显示目录，不显示文件 |
| `-a` | 显示隐藏文件（以 `.` 开头） |
| `-I "pattern"` | 排除匹配模式的文件/目录（如 `-I "*.o\|build"`） |
| `-P "pattern"` | 仅显示匹配模式的文件（如 `-P "*.c"`） |
| `-f` | 显示完整路径 |
| `--dirsfirst` | 目录排在文件前面（默认行为） |
| `-J` | 输出为 JSON 格式 |
| `-o file.txt` | 将结果保存到指定文件 |

---

## 三、实用组合示例

| 场景 | 命令 |
|------|------|
| 查看项目结构（忽略编译产物） | `tree -L 3 -I "build\|*.o\|*.dylib"` |
| 只看 C/C++ 源码 | `tree -P "*.{c,cpp,h}" --prune` |
| 导出结构到文档 | `tree -L 4 -I ".git\|node_modules" -o project_structure.txt` |
| 查看系统配置文件 | `tree /etc -P "*.conf" -L 2` |

> 💡 `--prune`：自动剪枝，不显示空目录。

---

## 四、安装方法

| 系统 | 安装命令 |
|------|--------|
| **macOS** | `brew install tree` |
| **Ubuntu / Debian** | `sudo apt install tree` |
| **CentOS / RHEL (≤7)** | `sudo yum install tree` |
| **Fedora / CentOS 8+** | `sudo dnf install tree` |

---
  